const connect = require(__dirname + '/connect/connect');
const model = require(__dirname + '/models/models');
const middleware = require(__dirname + '/middleware/middleware');
const route = require(__dirname + '/route/route');

route.app.listen(3000);